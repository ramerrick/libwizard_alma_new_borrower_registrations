# libwizard_alma_new_borrower_registrations
Script that retrieves new borrower information from a libwizard form, processes the data into a JSON object and then sends to data to Alma to create a new user.
